{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "The settings window, and only it, may hand a URL to the system browser.",
  "windows": ["main"],
  "permissions": ["opener:allow-open-url"]
}
